import { createFileRoute } from "@tanstack/react-router";
import { useEffect, useMemo, useState } from "react";
import { FolderTree, ImageOff, Search } from "lucide-react";
import { supabase } from "@/lib/supabase";
import { categories as presentationCategories } from "@/lib/shop-data";
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card";
import { Input } from "@/components/ui/input";

export const Route = createFileRoute("/admin/categories")({ component: AdminCategories });
type CategoryRow={id:string;name:string;slug:string;description:string|null;image_url:string|null;sort_order:number|null;active:boolean};type ProductRow={id:string;category_id:string;active:boolean};
function AdminCategories(){const[categories,setCategories]=useState<CategoryRow[]>([]);const[products,setProducts]=useState<ProductRow[]>([]);const[query,setQuery]=useState("");const[loading,setLoading]=useState(true);const[error,setError]=useState("");useEffect(()=>{let cancelled=false;void Promise.all([supabase.from("categories").select("id,name,slug,description,image_url,sort_order,active").order("sort_order"),supabase.from("products").select("id,category_id,active")]).then(([c,p])=>{if(cancelled)return;const failure=c.error||p.error;if(failure)setError(failure.message||"Unable to load categories.");else{setCategories((c.data??[]) as CategoryRow[]);setProducts((p.data??[]) as ProductRow[])}setLoading(false)});return()=>{cancelled=true}},[]);const rows=useMemo(()=>categories.map(c=>{const mapped=presentationCategories.find(x=>x.slug===c.slug);return{...c,image:mapped?.image||c.image_url||"",productCount:products.filter(p=>p.category_id===c.id).length,activeProductCount:products.filter(p=>p.category_id===c.id&&p.active).length}}),[categories,products]);const filtered=rows.filter(c=>{const q=query.trim().toLowerCase();return!q||c.name.toLowerCase().includes(q)||c.slug.toLowerCase().includes(q)||(c.description??"").toLowerCase().includes(q)});return <div className="space-y-6"><div><p className="text-sm font-medium text-muted-foreground">Catalogue</p><h2 className="mt-1 text-3xl font-semibold tracking-tight">Categories</h2><p className="mt-2 text-muted-foreground">Live category records and linked product counts from the same Supabase catalogue used by the storefront.</p></div><Card><CardHeader className="gap-4 sm:flex-row sm:items-center sm:justify-between"><div><CardTitle>All categories</CardTitle><CardDescription>{loading?"Loading categories...":`${filtered.length} of ${categories.length} categories`}</CardDescription></div><div className="relative w-full sm:w-72"><Search className="absolute left-3 top-3 size-4 text-muted-foreground"/><Input value={query} onChange={e=>setQuery(e.target.value)} placeholder="Search categories" className="pl-9"/></div></CardHeader><CardContent>{error?<p className="text-sm text-destructive">{error}</p>:loading?<p className="py-8 text-sm text-muted-foreground">Loading categories…</p>:filtered.length===0?<p className="py-8 text-sm text-muted-foreground">No categories found.</p>:<div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-3">{filtered.map(c=><div key={c.id} className="overflow-hidden rounded-xl border bg-background"><div className="aspect-[16/8] bg-muted">{c.image?<img src={c.image} alt={c.name} className="h-full w-full object-cover"/>:<div className="flex h-full items-center justify-center text-muted-foreground"><ImageOff className="size-6"/></div>}</div><div className="space-y-3 p-4"><div className="flex items-start justify-between gap-3"><div className="flex items-center gap-2 font-medium"><FolderTree className="size-4 text-muted-foreground"/>{c.name}</div><span className="rounded-full border px-2 py-1 text-xs">{c.active?"Active":"Inactive"}</span></div><p className="text-sm text-muted-foreground">{c.description||"No description"}</p><div className="border-t pt-3 text-xs text-muted-foreground"><p>/{c.slug}</p><p className="mt-1">{c.productCount} products · {c.activeProductCount} active</p></div></div></div>)}</div>}</CardContent></Card></div>}
