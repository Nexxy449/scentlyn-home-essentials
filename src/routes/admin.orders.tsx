import { createFileRoute } from "@tanstack/react-router";
import { useEffect, useState } from "react";
import { ReceiptText, Search } from "lucide-react";
import { supabase } from "@/lib/supabase";
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card";
import { Input } from "@/components/ui/input";

export const Route=createFileRoute("/admin/orders")({component:AdminOrders});
type Customer={full_name:string|null;email:string|null;phone:string|null};type Order={id:string;order_number:string;status:string;total:number|null;created_at:string;customers:Customer|null};
function AdminOrders(){const[orders,setOrders]=useState<Order[]>([]);const[query,setQuery]=useState("");const[loading,setLoading]=useState(true);const[error,setError]=useState("");useEffect(()=>{let cancelled=false;void supabase.from("orders").select("id,order_number,status,total,created_at,customers(full_name,email,phone)").order("created_at",{ascending:false}).then(({data,error})=>{if(cancelled)return;if(error)setError(error.message||"Unable to load orders.");else setOrders((data??[]) as Order[]);setLoading(false)});return()=>{cancelled=true}},[]);const filtered=orders.filter(o=>{const q=query.trim().toLowerCase();const c=o.customers;return!q||o.order_number.toLowerCase().includes(q)||c?.full_name?.toLowerCase().includes(q)||c?.email?.toLowerCase().includes(q)});return <div className="space-y-6"><div><p className="text-sm font-medium text-muted-foreground">Commerce</p><h2 className="mt-1 text-3xl font-semibold tracking-tight">Orders</h2><p className="mt-2 text-muted-foreground">Live order records from the same Supabase orders and customers tables used by checkout.</p></div><Card><CardHeader className="gap-4 sm:flex-row sm:items-center sm:justify-between"><div><CardTitle>All orders</CardTitle><CardDescription>{loading?"Loading orders...":`${filtered.length} of ${orders.length} orders`}</CardDescription></div><div className="relative w-full sm:w-72"><Search className="absolute left-3 top-3 size-4 text-muted-foreground"/><Input value={query} onChange={e=>setQuery(e.target.value)} placeholder="Search orders or customers" className="pl-9"/></div></CardHeader><CardContent>{error?<p className="text-sm text-destructive">{error}</p>:loading?<p className="py-8 text-sm text-muted-foreground">Loading orders…</p>:filtered.length===0?<p className="py-8 text-sm text-muted-foreground">No orders found yet.</p>:<div className="overflow-x-auto"><table className="w-full min-w-[760px] text-sm"><thead className="border-b text-left text-muted-foreground"><tr><th className="pb-3 font-medium">Order</th><th className="pb-3 font-medium">Customer</th><th className="pb-3 font-medium">Status</th><th className="pb-3 font-medium">Total</th><th className="pb-3 font-medium">Date</th></tr></thead><tbody>{filtered.map(o=><tr key={o.id} className="border-b last:border-0"><td className="py-4 font-medium"><span className="inline-flex items-center gap-2"><ReceiptText className="size-4 text-muted-foreground"/>{o.order_number}</span></td><td className="py-4"><p>{o.customers?.full_name||"Guest / unknown"}</p><p className="text-xs text-muted-foreground">{o.customers?.email||o.customers?.phone||"—"}</p></td><td className="py-4"><span className="rounded-full border px-2 py-1 text-xs capitalize">{o.status}</span></td><td className="py-4">KSh {Number(o.total??0).toLocaleString()}</td><td className="py-4 text-muted-foreground">{new Date(o.created_at).toLocaleDateString()}</td></tr>)}</tbody></table></div>}</CardContent></Card></div>}
