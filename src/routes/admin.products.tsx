import { createFileRoute } from "@tanstack/react-router";
import { useEffect, useMemo, useState } from "react";
import { Pencil, Plus, Trash2, Upload, X } from "lucide-react";
import { supabase } from "@/lib/supabase";
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card";
import { Input } from "@/components/ui/input";
import { Button } from "@/components/ui/button";
import { products as catalogueProducts } from "@/lib/shop-data";

type V={id:string;name:string;sku:string|null;price:number;compare_at_price:number|null;stock_quantity:number;image_url:string|null;active:boolean};
type P={id:string;category_id:string|null;name:string;slug:string;brand:string|null;description:string|null;image_url:string|null;featured:boolean;active:boolean;product_variants:V[]|null};
type C={id:string;name:string;slug:string};
type DV={id?:string;name:string;sku:string;price:string;compare_at_price:string;stock_quantity:string;image_url:string;imageFile?:File;active:boolean};
type D={id?:string;category_id:string;name:string;slug:string;brand:string;description:string;image_url:string;imageFile?:File;featured:boolean;active:boolean;variants:DV[]};

export const Route=createFileRoute("/admin/products")({component:AdminProducts});
const labels:Record<string,string>={laundry:"Laundry",kitchen:"Kitchen",bathroom:"Toiletries",scents:"Fragrance"};
const empty=():D=>({category_id:"",name:"",slug:"",brand:"",description:"",image_url:"",featured:false,active:true,variants:[{name:"Default",sku:"",price:"0",compare_at_price:"",stock_quantity:"0",image_url:"",active:true}]});
const ext=(f:File)=>f.name.split(".").pop()?.toLowerCase().replace(/[^a-z0-9]/g,"")||"jpg";
const pathOf=(url?:string|null)=>{if(!url)return null;const m="/storage/v1/object/public/product-images/",i=url.indexOf(m);return i<0?null:decodeURIComponent(url.slice(i+m.length)).split("?")[0]};
async function upload(file:File,path:string){if(!["image/jpeg","image/png","image/webp"].includes(file.type))throw new Error("Please upload a JPEG, PNG or WebP image.");if(file.size>5*1024*1024)throw new Error("Each image must be 5 MB or smaller.");const {error}=await supabase.storage.from("product-images").upload(path,file,{upsert:true,cacheControl:"31536000",contentType:file.type});if(error)throw error;const {data}=supabase.storage.from("product-images").getPublicUrl(path);if(!data.publicUrl)throw new Error("The image URL could not be created.");return data.publicUrl}
function Picker({value,onChange,label}:{value:string;onChange:(f:File|null)=>void;label:string}){return <div className="space-y-2"><p className="text-sm font-medium">{label}</p><div className="flex items-center gap-3"><label className="flex cursor-pointer items-center gap-2 rounded-md border px-3 py-2 text-sm hover:bg-secondary"><Upload className="size-4"/>Upload image<input type="file" accept="image/jpeg,image/png,image/webp" className="hidden" onChange={e=>onChange(e.target.files?.[0]??null)}/></label>{value&&<div className="relative size-20 overflow-hidden rounded-lg border bg-muted"><img src={value} alt="Preview" className="size-full object-cover" width="80" height="80"/><button type="button" className="absolute right-1 top-1 rounded-full bg-background/90 p-1" onClick={()=>onChange(null)} aria-label={`Remove ${label}`}><X className="size-3"/></button></div>}</div></div>}
function AdminProducts(){
 const [products,setProducts]=useState<P[]>([]),[categories,setCategories]=useState<C[]>([]),[query,setQuery]=useState(""),[editing,setEditing]=useState<D|null>(null),[busy,setBusy]=useState(false),[error,setError]=useState("");
 const load=async()=>{const[{data:p,error:pe},{data:c,error:ce}]=await Promise.all([supabase.from("products").select("id,category_id,name,slug,brand,description,image_url,featured,active,product_variants(id,name,sku,price,compare_at_price,stock_quantity,image_url,active)").order("name"),supabase.from("categories").select("id,name,slug").order("sort_order")]);if(pe||ce)setError((pe||ce)?.message||"Unable to load catalogue");else{setProducts((p??[])as P[]);setCategories((c??[])as C[]);}};
 useEffect(()=>{void load()},[]);
 const shown=useMemo(()=>products.filter(p=>`${p.name} ${p.slug} ${p.brand||""}`.toLowerCase().includes(query.trim().toLowerCase())),[products,query]);
 const display=(p:P)=>p.image_url||p.product_variants?.find(v=>v.active&&v.image_url)?.image_url||catalogueProducts.find(x=>x.slug===p.slug)?.image||null;
 const startPrice=(p:P)=>Math.min(...(p.product_variants||[]).filter(v=>v.active).map(v=>v.price),Infinity);
 const openNew=()=>{setError("");setEditing(empty())};
 const openEdit=(p:P)=>{setError("");setEditing({id:p.id,category_id:p.category_id||"",name:p.name,slug:p.slug,brand:p.brand||"",description:p.description||"",image_url:p.image_url||"",featured:p.featured,active:p.active,variants:(p.product_variants||[]).map(v=>({id:v.id,name:v.name,sku:v.sku||"",price:String(v.price),compare_at_price:v.compare_at_price==null?"":String(v.compare_at_price),stock_quantity:String(v.stock_quantity),image_url:v.image_url||"",active:v.active}))})};
 const patch=(x:Partial<D>)=>setEditing(e=>e?{...e,...x}:e); const patchV=(i:number,x:Partial<DV>)=>setEditing(e=>e?{...e,variants:e.variants.map((v,n)=>n===i?{...v,...x}:v)}:e);
 const save=async()=>{if(!editing||busy)return;setBusy(true);setError("");const uploaded:string[]=[];const old=editing.id?products.find(p=>p.id===editing.id):undefined;const oldPaths=[pathOf(old?.image_url),...(old?.product_variants||[]).map(v=>pathOf(v.image_url))].filter((x):x is string=>!!x);try{const name=editing.name.trim(),slug=editing.slug.trim().toLowerCase();if(!name||!slug)throw new Error("Product name and slug are required.");if(!editing.variants.length)throw new Error("Add at least one variant.");const id=editing.id||crypto.randomUUID();let main=editing.imageFile?"":editing.image_url.trim();if(editing.imageFile){const p=`products/${id}/main-${Date.now()}.${ext(editing.imageFile)}`;main=await upload(editing.imageFile,p);uploaded.push(p)}const vars=[] as Record<string,unknown>[];for(const v of editing.variants){const vid=v.id||crypto.randomUUID();let img=v.imageFile?"":v.image_url.trim();const price=Number(v.price),stock=Number(v.stock_quantity),compare=v.compare_at_price.trim()?Number(v.compare_at_price):null;if(!v.name.trim()||!Number.isFinite(price)||price<0||!Number.isInteger(stock)||stock<0||(compare!==null&&(!Number.isFinite(compare)||compare<price)))throw new Error("Each variant needs a name, valid price/stock, and a compare price equal to or above the selling price.");if(v.imageFile){const p=`products/${id}/variants/${vid}-${Date.now()}-${Math.random().toString(36).slice(2,7)}.${ext(v.imageFile)}`;img=await upload(v.imageFile,p);uploaded.push(p)}vars.push({id:vid,name:v.name.trim(),sku:v.sku.trim(),price,compare_at_price:compare,stock_quantity:stock,image_url:img,active:v.active})}const{error:e}=await supabase.rpc("admin_save_product",{p_product:{id,category_id:editing.category_id||null,name,slug,brand:editing.brand.trim()||null,description:editing.description.trim()||null,image_url:main||null,featured:editing.featured,active:editing.active},p_variants:vars});if(e)throw e;const keep=new Set([pathOf(main),...vars.map(v=>pathOf(typeof v.image_url==="string"?v.image_url:null))].filter((x):x is string=>!!x));const stale=oldPaths.filter(x=>!keep.has(x));if(stale.length)await supabase.storage.from("product-images").remove(stale);setEditing(null);await load()}catch(e){if(uploaded.length)await supabase.storage.from("product-images").remove(uploaded);setError(e instanceof Error?e.message:"Unable to save product.")}finally{setBusy(false)}};
 const active=async(p:P,on:boolean)=>{if(busy)return;setBusy(true);setError("");const{error:e}=await supabase.rpc("admin_set_product_active",{p_product_id:p.id,p_active:on});if(e)setError(e.message);else await load();setBusy(false)};
 return <div className="space-y-6"><div className="flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><p className="text-sm text-muted-foreground">Catalogue</p><h2 className="text-3xl font-semibold">Products</h2><p className="mt-2 text-muted-foreground">Manage products, variants, prices, stock and images used by the storefront.</p></div><Button onClick={openNew}><Plus className="mr-2 size-4"/>Add product</Button></div>{error&&<p role="alert" className="rounded border border-destructive/30 p-3 text-sm text-destructive">{error}</p>}<Card><CardHeader><div className="flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-between"><div><CardTitle>Catalogue</CardTitle><CardDescription>{shown.length} products</CardDescription></div><Input className="sm:max-w-xs" placeholder="Search products" value={query} onChange={e=>setQuery(e.target.value)}/></div></CardHeader><CardContent><div className="space-y-3">{shown.map(p=>{const price=startPrice(p);return <div key={p.id} className="flex flex-col gap-3 rounded-lg border p-3 sm:flex-row sm:items-center sm:justify-between"><div className="flex min-w-0 items-center gap-3"><div className="size-16 shrink-0 overflow-hidden rounded-lg border bg-muted">{display(p)?<img src={display(p)!} alt={p.name} className="size-full object-cover"/>:<span className="flex size-full items-center justify-center text-xs text-muted-foreground">No image</span>}</div><div className="min-w-0"><p className="font-medium">{p.name}</p><p className="text-xs text-muted-foreground">/{p.slug} · {(p.product_variants||[]).reduce((n,v)=>n+v.stock_quantity,0)} units · {(p.product_variants||[]).length} variants · {p.active?"Active":"Inactive"}</p>{Number.isFinite(price)&&<p className="mt-1 text-sm font-semibold text-primary">From {price.toLocaleString("en-KE",{style:"currency",currency:"KES",maximumFractionDigits:0})}</p>}</div></div><div className="flex gap-2"><Button type="button" size="sm" variant="outline" onClick={()=>openEdit(p)}><Pencil className="size-4"/><span className="sr-only">Edit {p.name}</span></Button><Button type="button" size="sm" variant="outline" disabled={busy} onClick={()=>void active(p,!p.active)}>{p.active?"Deactivate":"Activate"}</Button></div></div>})}</div></CardContent></Card>{editing&&<Card><CardHeader><CardTitle>{editing.id?"Edit product":"Add product"}</CardTitle><CardDescription>Upload images directly from your phone or computer. Changes are saved together so a failed save does not leave half-written product data.</CardDescription></CardHeader><CardContent className="space-y-6"><div className="grid gap-4 sm:grid-cols-2"><Input aria-label="Product name" placeholder="Product name" value={editing.name} onChange={e=>{const n=e.target.value;patch({name:n,slug:editing.id?editing.slug:n.toLowerCase().trim().replace(/[^a-z0-9]+/g,"-").replace(/^-|-$/g,"")})}}/><Input aria-label="Slug" placeholder="Slug" value={editing.slug} onChange={e=>patch({slug:e.target.value})}/><select aria-label="Category" className="h-10 rounded-md border bg-background px-3" value={editing.category_id} onChange={e=>patch({category_id:e.target.value})}><option value="">No category</option>{categories.map(c=><option key={c.id} value={c.id}>{labels[c.slug]||c.name}</option>)}</select><Input aria-label="Brand" placeholder="Brand" value={editing.brand} onChange={e=>patch({brand:e.target.value})}/><Input aria-label="Description" placeholder="Description" value={editing.description} onChange={e=>patch({description:e.target.value})}/></div><Picker value={editing.image_url} label="Main product image" onChange={f=>f?patch({imageFile:f,image_url:URL.createObjectURL(f)}):patch({imageFile:undefined,image_url:""})}/><div className="rounded-xl border bg-surface/50 p-4"><div className="flex items-center justify-between gap-4"><div><p className="font-medium">Main product price</p><p className="text-sm text-muted-foreground">The main price uses the first active variant.</p></div>{editing.variants[0]&&<Input aria-label="Main product price" type="number" min="0" step="0.01" className="w-40" value={editing.variants[0].price} onChange={e=>patchV(0,{price:e.target.value})}/>}</div></div><div className="flex gap-5 text-sm"><label><input type="checkbox" checked={editing.active} onChange={e=>patch({active:e.target.checked})}/> Active</label><label><input type="checkbox" checked={editing.featured} onChange={e=>patch({featured:e.target.checked})}/> Featured</label></div><div><div className="mb-3 flex items-center justify-between"><div><h3 className="font-medium">Variants</h3><p className="text-sm text-muted-foreground">Each variant can have its own name, price, stock and image.</p></div><Button type="button" size="sm" variant="outline" onClick={()=>setEditing(d=>d?{...d,variants:[...d.variants,{name:"",sku:"",price:"0",compare_at_price:"",stock_quantity:"0",image_url:"",active:true}]}:d)}><Plus className="mr-1 size-4"/>Variant</Button></div>{editing.variants.map((v,i)=><div key={v.id||i} className="mb-4 rounded-lg border p-4"><div className="mb-3 flex items-center justify-between"><p className="text-sm font-medium">Variant {i+1}</p>{editing.variants.length>1&&<Button type="button" size="sm" variant="ghost" onClick={()=>setEditing(d=>d?{...d,variants:d.variants.filter((_,n)=>n!==i)}:d)}><Trash2 className="size-4"/></Button>}</div><div className="grid gap-3 sm:grid-cols-3"><Input aria-label={`Variant ${i+1} name`} placeholder="Variant name / scent / pack" value={v.name} onChange={e=>patchV(i,{name:e.target.value})}/><Input aria-label={`Variant ${i+1} SKU`} placeholder="SKU" value={v.sku} onChange={e=>patchV(i,{sku:e.target.value})}/><Input aria-label={`Variant ${i+1} price`} type="number" min="0" step="0.01" placeholder="Price" value={v.price} onChange={e=>patchV(i,{price:e.target.value})}/><Input aria-label={`Variant ${i+1} compare price`} type="number" min="0" step="0.01" placeholder="Compare price" value={v.compare_at_price} onChange={e=>patchV(i,{compare_at_price:e.target.value})}/><Input aria-label={`Variant ${i+1} stock`} type="number" min="0" step="1" placeholder="Stock" value={v.stock_quantity} onChange={e=>patchV(i,{stock_quantity:e.target.value})}/><label className="self-center text-sm"><input type="checkbox" checked={v.active} onChange={e=>patchV(i,{active:e.target.checked})}/> Active</label></div><div className="mt-4"><Picker value={v.image_url} label="Variant image" onChange={f=>f?patchV(i,{imageFile:f,image_url:URL.createObjectURL(f)}):patchV(i,{imageFile:undefined,image_url:""})}/></div></div>)}</div><div className="flex gap-2"><Button disabled={busy} onClick={()=>void save()}>{busy?"Saving…":"Save product"}</Button><Button type="button" variant="outline" disabled={busy} onClick={()=>setEditing(null)}>Cancel</Button></div></CardContent></Card>}</div>;
}
