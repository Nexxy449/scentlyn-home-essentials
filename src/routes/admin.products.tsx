import { createFileRoute } from "@tanstack/react-router";
import { useEffect, useMemo, useState } from "react";
import { Pencil, Plus, Trash2 } from "lucide-react";
import { supabase } from "@/lib/supabase";
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card";
import { Input } from "@/components/ui/input";
import { Button } from "@/components/ui/button";
export const Route=createFileRoute("/admin/products")({component:AdminProducts});
type V={id:string;name:string;sku:string|null;price:number;compare_at_price:number|null;stock_quantity:number;active:boolean};type P={id:string;category_id:string|null;name:string;slug:string;brand:string|null;description:string|null;image_url:string|null;featured:boolean;active:boolean;product_variants:V[]|null};type C={id:string;name:string};type DraftV={id?:string;name:string;sku:string;price:string;compare_at_price:string;stock_quantity:string;active:boolean};type Draft={id?:string;category_id:string;name:string;slug:string;brand:string;description:string;image_url:string;featured:boolean;active:boolean;variants:DraftV[]};const empty=():Draft=>({category_id:"",name:"",slug:"",brand:"",description:"",image_url:"",featured:false,active:true,variants:[{name:"Default",sku:"",price:"0",compare_at_price:"",stock_quantity:"0",active:true}]});
function AdminProducts(){const[products,setProducts]=useState<P[]>([]),[categories,setCategories]=useState<C[]>([]),[query,setQuery]=useState(""),[editing,setEditing]=useState<Draft|null>(null),[busy,setBusy]=useState(false),[error,setError]=useState("");const load=async()=>{const[{data:p,error:pe},{data:c,error:ce}]=await Promise.all([supabase.from("products").select("id,category_id,name,slug,brand,description,image_url,featured,active,product_variants(id,name,sku,price,compare_at_price,stock_quantity,active)").order("name"),supabase.from("categories").select("id,name").order("sort_order")]);if(pe||ce)setError((pe||ce)?.message||"Unable to load catalogue");else{setProducts((p??[])as P[]);setCategories((c??[])as C[])} };useEffect(()=>{void load()},[]);const shown=useMemo(()=>products.filter(p=>`${p.name} ${p.slug} ${p.brand||""}`.toLowerCase().includes(query.toLowerCase())),[products,query]);const openNew=()=>{setError("");setEditing(empty())};const openEdit=(p:P)=>{setError("");setEditing({id:p.id,category_id:p.category_id||"",name:p.name,slug:p.slug,brand:p.brand||"",description:p.description||"",image_url:p.image_url||"",featured:p.featured,active:p.active,variants:(p.product_variants||[]).map(v=>({id:v.id,name:v.name,sku:v.sku||"",price:String(v.price),compare_at_price:v.compare_at_price==null?"":String(v.compare_at_price),stock_quantity:String(v.stock_quantity),active:v.active}))})};const save=async()=>{if(!editing||busy)return;setBusy(true);setError("");if(!editing.name.trim()||!editing.slug.trim()){setError("Product name and slug are required.");setBusy(false);return}if(editing.variants.some(v=>!v.name.trim()||Number(v.price)<0||Number(v.stock_quantity)<0)){setError("Each variant needs a name and valid non-negative price and stock.");setBusy(false);return}const row={category_id:editing.category_id||null,name:editing.name.trim(),slug:editing.slug.trim(),brand:editing.brand.trim()||null,description:editing.description.trim()||null,image_url:editing.image_url.trim()||null,featured:editing.featured,active:editing.active};let id=editing.id;if(id){const r=await supabase.from("products").update(row).eq("id",id);if(r.error){setError(r.error.message);setBusy(false);return}}else{const r=await supabase.from("products").insert(row).select("id").single();if(r.error||!r.data){setError(r.error?.message||"Unable to create product.");setBusy(false);return}id=r.data.id}for(const v of editing.variants){const vr={product_id:id,name:v.name.trim(),sku:v.sku.trim()||null,price:Number(v.price),compare_at_price:v.compare_at_price.trim()?Number(v.compare_at_price):null,stock_quantity:Number(v.stock_quantity),active:v.active};const r=v.id?await supabase.from("product_variants").update(vr).eq("id",v.id):await supabase.from("product_variants").insert(vr);if(r.error){setError(r.error.message);setBusy(false);return}}setEditing(null);await load();setBusy(false)};const remove=async(p:P)=>{if(!window.confirm(`Delete ${p.name}? This cannot be undone.`))return;setBusy(true);setError("");const r=await supabase.from("products").delete().eq("id",p.id);if(r.error)setError(r.error.message);else await load();setBusy(false)};const update=(patch:Partial<Draft>)=>setEditing(e=>e?{...e,...patch}:e);const updateV=(i:number,patch:Partial<DraftV>)=>setEditing(e=>e?{...e,variants:e.variants.map((v,n)=>n===i?{...v,...patch}:v)}:e);return <div className="space-y-6"><div className="flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><p className="text-sm text-muted-foreground">Catalogue</p><h2 className="text-3xl font-semibold">Products</h2><p className="mt-2 text-muted-foreground">Manage the same products, variants, prices and stock used by the storefront.</p></div><Button type="button" onClick={openNew}><Plus className="mr-2 size-4"/>Add product</Button></div>{error&&<p className="rounded border border-destructive/30 p-3 text-sm text-destructive">{error}</p>}<Card><CardHeader><div className="flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-between"><div><CardTitle>Inventory</CardTitle><CardDescription>{shown.length} products</CardDescription></div><Input className="sm:max-w-xs" placeholder="Search products" value={query} onChange={e=>setQuery(e.target.value)}/></div></CardHeader><CardContent><div className="space-y-3">{shown.map(p=><div key={p.id} className="flex items-center justify-between gap-4 rounded-lg border p-3"><div className="flex min-w-0 items-center gap-3">{p.image_url&&<img src={p.image_url} alt="" className="size-14 rounded object-cover"/>}<div><p className="font-medium">{p.name}</p><p className="text-xs text-muted-foreground">/{p.slug} · {(p.product_variants||[]).reduce((n,v)=>n+v.stock_quantity,0)} units · {(p.product_variants||[]).length} variants</p></div></div><div className="flex gap-2"><Button type="button" size="sm" variant="outline" onClick={()=>openEdit(p)}><Pencil className="size-4"/></Button><Button type="button" size="sm" variant="outline" disabled={busy} onClick={()=>void remove(p)}><Trash2 className="size-4"/></Button></div></div>)}</div></CardContent></Card>{editing&&<Card><CardHeader><CardTitle>{editing.id?"Edit product":"Add product"}</CardTitle><CardDescription>Changes are saved directly to the existing Supabase catalogue.</CardDescription></CardHeader><CardContent className="space-y-5"><div className="grid gap-3 sm:grid-cols-2"><Input placeholder="Product name" value={editing.name} onChange={e=>{const name=e.target.value;update({name,slug:editing.id?editing.slug:name.toLowerCase().trim().replace(/[^a-z0-9]+/g,"-").replace(/^-|-$/g,"")})}}/><Input placeholder="Slug" value={editing.slug} onChange={e=>update({slug:e.target.value})}/><select className="h-10 rounded-md border bg-background px-3" value={editing.category_id} onChange={e=>update({category_id:e.target.value})}><option value="">No category</option>{categories.map(c=><option key={c.id} value={c.id}>{c.name}</option>)}</select><Input placeholder="Brand" value={editing.brand} onChange={e=>update({brand:e.target.value})}/><Input placeholder="Image URL" value={editing.image_url} onChange={e=>update({image_url:e.target.value})}/><Input placeholder="Description" value={editing.description} onChange={e=>update({description:e.target.value})}/></div><div className="flex gap-5 text-sm"><label><input type="checkbox" checked={editing.active} onChange={e=>update({active:e.target.checked})}/> Active</label><label><input type="checkbox" checked={editing.featured} onChange={e=>update({featured:e.target.checked})}/> Featured</label></div><div><div className="mb-2 flex items-center justify-between"><h3 className="font-medium">Variants</h3><Button type="button" size="sm" variant="outline" onClick={()=>setEditing(d=>d?{...d,variants:[...d.variants,{name:"",sku:"",price:"0",compare_at_price:"",stock_quantity:"0",active:true}]}:d)}><Plus className="mr-1 size-4"/>Variant</Button></div>{editing.variants.map((v,i)=><div key={v.id||i} className="mb-3 grid gap-2 rounded border p-3 sm:grid-cols-3"><Input placeholder="Variant name" value={v.name} onChange={e=>updateV(i,{name:e.target.value})}/><Input placeholder="SKU" value={v.sku} onChange={e=>updateV(i,{sku:e.target.value})}/><Input type="number" min="0" placeholder="Price" value={v.price} onChange={e=>updateV(i,{price:e.target.value})}/><Input type="number" min="0" placeholder="Compare price" value={v.compare_at_price} onChange={e=>updateV(i,{compare_at_price:e.target.value})}/><Input type="number" min="0" placeholder="Stock" value={v.stock_quantity} onChange={e=>updateV(i,{stock_quantity:e.target.value})}/><label className="self-center text-sm"><input type="checkbox" checked={v.active} onChange={e=>updateV(i,{active:e.target.checked})}/> Active</label></div>)}</div><div className="flex gap-2"><Button type="button" disabled={busy} onClick={()=>void save()}>{busy?"Saving…":"Save product"}</Button><Button type="button" variant="outline" disabled={busy} onClick={()=>setEditing(null)}>Cancel</Button></div></CardContent></Card>}</div>}
