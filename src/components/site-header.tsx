import { Link, useNavigate } from "@tanstack/react-router";
import { Menu, Search, ShoppingBag, X } from "lucide-react";
import { useEffect, useMemo, useRef, useState } from "react";
import { createPortal } from "react-dom";

import { Button } from "@/components/ui/button";
import { useCart } from "@/lib/cart";
import { getCatalogue } from "@/lib/catalogue";
import { categories, formatPrice, fromPrice } from "@/lib/shop-data";

const categoryNav = [
  { slug: "laundry", label: "Laundry" },
  { slug: "kitchen", label: "Kitchen" },
  { slug: "bathroom", label: "Toiletries" },
  { slug: "scents", label: "Fragrance" },
] as const;
const pages = [{ to: "/about", label: "About" }, { to: "/faq", label: "FAQ" }, { to: "/contact", label: "Contact" }] as const;

export function SiteHeader() {
  const [menuOpen, setMenuOpen] = useState(false); const [searchOpen, setSearchOpen] = useState(false); const { count } = useCart();
  return <header className="sticky top-0 z-50 w-full border-b border-border bg-background/95 backdrop-blur-xl"><div className="bg-primary px-4 py-2 text-center text-[10px] font-semibold uppercase tracking-[0.2em] text-primary-foreground sm:text-[11px]">Delivery across Kenya <span className="mx-2 opacity-40">•</span> 100% original products <span className="mx-2 opacity-40">•</span> Secure payments</div><div className="mx-auto flex h-[74px] max-w-7xl items-center px-4 sm:px-8"><button type="button" aria-label="Open menu" onClick={() => setMenuOpen(true)} className="mr-2 inline-flex h-10 w-10 items-center justify-center rounded-full hover:bg-secondary md:hidden"><Menu className="h-5 w-5" /></button><Link to="/" className="group flex shrink-0 flex-col leading-none" aria-label="Scentlyn Laundrymart home"><span className="font-display text-[1.55rem] font-semibold tracking-[0.08em] text-foreground sm:text-[1.8rem]">SCENTLYN</span><span className="mt-1 text-[8px] font-semibold tracking-[0.34em] text-primary sm:text-[9px]">LAUNDRYMART</span></Link><nav className="mx-auto hidden items-center gap-1 lg:flex" aria-label="Main">{categoryNav.map((item) => <Link key={item.slug} to="/category/$category" params={{ category: item.slug }} className="rounded-full px-4 py-2.5 text-sm font-semibold text-muted-foreground transition-colors hover:bg-secondary hover:text-foreground" activeProps={{ className: "bg-secondary text-foreground" }}>{item.label}</Link>)}<Link to="/shop" className="rounded-full px-4 py-2.5 text-sm font-semibold text-muted-foreground transition-colors hover:bg-secondary hover:text-foreground" activeProps={{ className: "bg-secondary text-foreground" }}>Shop All</Link></nav><div className="ml-auto flex items-center gap-1"><button type="button" aria-label="Search products" onClick={() => setSearchOpen(true)} className="inline-flex h-10 w-10 items-center justify-center rounded-full hover:bg-secondary"><Search className="h-5 w-5" /></button><Link to="/cart" aria-label={`Cart, ${count} item${count === 1 ? "" : "s"}`} className="relative inline-flex h-10 w-10 items-center justify-center rounded-full hover:bg-secondary"><ShoppingBag className="h-5 w-5" />{count > 0 && <span className="absolute right-0.5 top-0.5 flex h-4 min-w-4 items-center justify-center rounded-full bg-primary px-1 text-[10px] font-bold text-primary-foreground">{count}</span>}</Link></div></div><div className="hidden border-t border-border bg-background md:block lg:hidden"><nav className="mx-auto flex max-w-7xl items-center justify-center gap-1 px-4 py-2" aria-label="Main">{categoryNav.map((item) => <Link key={item.slug} to="/category/$category" params={{ category: item.slug }} className="rounded-full px-4 py-1.5 text-xs font-semibold text-muted-foreground hover:bg-secondary hover:text-foreground">{item.label}</Link>)}<Link to="/shop" className="rounded-full px-4 py-1.5 text-xs font-semibold text-muted-foreground hover:bg-secondary hover:text-foreground">Shop All</Link></nav></div>{menuOpen && <MobileMenu count={count} onClose={() => setMenuOpen(false)} />}{searchOpen && <SearchOverlay onClose={() => setSearchOpen(false)} />}</header>;
}
function Portal({ children }: { children: React.ReactNode }) { const [mounted, setMounted] = useState(false); useEffect(() => setMounted(true), []); if (!mounted) return null; return createPortal(children, document.body); }
function MobileMenu({ count, onClose }: { count: number; onClose: () => void }) { return <Portal><div className="fixed inset-0 z-[60] overflow-y-auto bg-background md:hidden"><div className="flex h-20 items-center justify-between border-b border-border px-5"><Link to="/" onClick={onClose} className="flex flex-col leading-none"><span className="font-display text-xl font-semibold tracking-[0.08em]">SCENTLYN</span><span className="mt-1 text-[8px] font-semibold tracking-[0.34em] text-primary">LAUNDRYMART</span></Link><button type="button" aria-label="Close menu" onClick={onClose} className="inline-flex h-10 w-10 items-center justify-center rounded-full hover:bg-secondary"><X className="h-5 w-5" /></button></div><nav className="flex flex-col p-5 pb-12"><Link to="/shop" onClick={onClose} className="rounded-xl bg-secondary px-4 py-4 text-base font-semibold">Shop all</Link><span className="mt-7 px-4 text-[10px] font-semibold uppercase tracking-[0.2em] text-muted-foreground">Shop by category</span>{categoryNav.map((item) => <Link key={item.slug} to="/category/$category" params={{ category: item.slug }} onClick={onClose} className="rounded-xl px-4 py-4 text-lg font-semibold hover:bg-secondary">{item.label}</Link>)}<span className="mt-7 px-4 text-[10px] font-semibold uppercase tracking-[0.2em] text-muted-foreground">More</span>{pages.map((page) => <Link key={page.to} to={page.to} onClick={onClose} className="rounded-xl px-4 py-3.5 text-base font-semibold hover:bg-secondary">{page.label}</Link>)}<Link to="/cart" onClick={onClose} className="mt-5 rounded-xl border border-border px-4 py-4 text-center text-base font-semibold">View cart{count > 0 ? ` · ${count} item${count === 1 ? "" : "s"}` : ""}</Link></nav></div></Portal>; }
function SearchOverlay({ onClose }: { onClose: () => void }) {
  const [query, setQuery] = useState(""); const [products, setProducts] = useState<Awaited<ReturnType<typeof getCatalogue>>["products"]>([]); const inputRef = useRef<HTMLInputElement>(null); const navigate = useNavigate();
  useEffect(() => { inputRef.current?.focus(); let active = true; void getCatalogue().then(c => { if (active) setProducts(c.products); }); return () => { active = false; }; }, []);
  const results = useMemo(() => { const q = query.trim().toLowerCase(); if (!q) return []; return products.filter(p => [p.name,p.brand,p.category,p.subcategory,p.short,p.description,...p.variants.map(v=>v.label)].filter(Boolean).some(field => String(field).toLowerCase().includes(q))).slice(0,6); }, [products, query]);
  return <Portal><div className="fixed inset-0 z-[60] overflow-y-auto bg-background"><div className="mx-auto max-w-3xl px-4 pb-16 pt-6 sm:px-6"><form onSubmit={(event) => { event.preventDefault(); onClose(); navigate({ to: "/search", search: { q: query } }); }} className="flex items-center gap-2 rounded-2xl border border-border bg-card px-3 shadow-card"><Search className="h-5 w-5 text-muted-foreground" /><label className="sr-only" htmlFor="site-search">Search products</label><input id="site-search" ref={inputRef} value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search Ariel, Vanish, Lenor, candles…" className="h-14 flex-1 bg-transparent text-base outline-none placeholder:text-muted-foreground" /><button type="button" aria-label="Close search" onClick={onClose} className="inline-flex h-10 w-10 items-center justify-center rounded-full hover:bg-secondary"><X className="h-5 w-5" /></button></form><div className="mt-4 space-y-2">{query && !products.length && <p className="px-2 py-6 text-center text-sm text-muted-foreground">Loading products…</p>}{query && products.length > 0 && results.length === 0 && <p className="px-2 py-6 text-center text-sm text-muted-foreground">No products match “{query}”. Try a brand name or a category.</p>}{results.map(product => <Link key={product.slug} to="/product/$slug" params={{ slug: product.slug }} onClick={onClose} className="flex items-center gap-3 rounded-xl border border-border bg-card p-2 hover:shadow-card"><img src={product.image} alt={product.name} className="h-14 w-14 rounded-lg object-cover" loading="lazy" /><span className="min-w-0 flex-1"><span className="block truncate text-sm font-semibold">{product.name}</span><span className="block truncate text-xs text-muted-foreground">{product.variants.map(variant => variant.label).join(" • ")}</span></span><span className="shrink-0 text-sm font-bold text-primary">from {formatPrice(fromPrice(product))}</span></Link>)}{query && results.length > 0 && <Button variant="soft" className="w-full" onClick={() => { onClose(); navigate({ to: "/search", search: { q: query } }); }}>See all results</Button>}</div></div></div></Portal>;
}
